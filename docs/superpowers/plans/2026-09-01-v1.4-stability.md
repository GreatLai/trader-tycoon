# v1.4 Stability Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Fix confirmed v1.4 correctness and compatibility defects without changing game balance.

**Architecture:** Load the existing classic scripts in a Node VM test harness, add targeted normalization at the save boundary, and keep behavior fixes inside their current modules. The browser delivery remains dependency-free.

**Tech Stack:** Browser JavaScript, Node.js built-in test runner, Node `vm`, HTML/CSS.

---

### Task 1: Regression Harness And Baseline

**Files:**
- Create: `tests/helpers/load-game.js`
- Create: `tests/game-regressions.test.js`
- Create: `package.json`

- [ ] Write tests for fresh-game unlocks, forced-liquidation settlement, day-90 termination, v1.3 ecological-save migration, panel hierarchy, event data, and deterministic simulation.
- [ ] Run `npm test` and verify the defect-specific tests fail for the expected reasons.

### Task 2: Rule And Economy Fixes

**Files:**
- Modify: `js/utils.js`
- Modify: `js/state.js`
- Modify: `js/events.js`
- Modify: `js/game.js`
- Modify: `js/ui.js`

- [ ] Make initial market selection independent of the previous global state.
- [ ] Deduct the daily-cost shortfall from forced-liquidation proceeds.
- [ ] Stop on day 90 before daily costs, market selection, events, and history mutate the state.
- [ ] Run focused regression tests and verify they pass.

### Task 3: Save Migration And Markup Repair

**Files:**
- Modify: `js/save.js`
- Modify: `js/state.js`
- Modify: `index.html`

- [ ] Stamp new states with the current app version.
- [ ] Normalize v1.3 active ecological-event progress and required collection fields during load.
- [ ] Close the warehouse panel before the bank panel.
- [ ] Run focused regression tests and verify they pass.

### Task 4: Full Verification

**Files:**
- Modify only if a test exposes another root cause within the approved scope.

- [ ] Run `npm test`.
- [ ] Run syntax checks for every production JavaScript file.
- [ ] Run browser smoke tests on desktop and mobile viewports.
- [ ] Review the final diff for scope and regressions.
- [ ] Commit and push `codex/v1.4-stability` without storing credentials in Git configuration.

